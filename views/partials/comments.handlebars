<div class="comments">
    {{#each this}}
        <section class="comment">
            <div class="meta">
                {{user.username}} on {{formatDate created}}
            </div>

            <div class="text">
                {{commentText}}
            </div>
        </section>
    {{/each}}
</div>