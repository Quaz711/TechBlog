<article class="post">
    <div class="title">
        <a href="/post/{{id}}">{{title}}</a>
    </div>

    <div class="body">
        {{postText}}
    </div>

    <div class="meta">
        <strong>{{user.username}} posted this on {{format_date created}}</strong>
    </div>
</article>