<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Kanit:wght@300&display=swap">
    <title>Tech Blog</title>
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <p class="blog-title">Tech Blog</p>
            </h1>

            <nav>
                {{#if loggedIn}}
                    <a class="homepage" href="/">Homepage</a>
                    <a href="/dashboard">Dashboard</a>
                    <button class="btn-no-style" id="logout">Log Out</button>
                {{else}}
                    <a class="homepage" href="/">Homepage</a>
                    <a href="/login">Log In</a>
                {{/if}}
            </nav>
        </header>

        <main>
            {{{body}}}
        </main>
    </div>

    <script src="/scripts/logout.js"></script>
</body>
</html>